
  # vegrin

  This is a code bundle for vegrin. The original project is available at https://www.figma.com/design/WBPjs8gdmTLBpBZt22rRaA/vegrin.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  